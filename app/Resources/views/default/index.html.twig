{% extends 'base.html.twig' %}

{% block body %}
    <h1>Welcome to Recipes</h1>
    {% for post in posts %}
        <h3>{{ post.title }}</h3>
        <h5>Ingredients:</h5>
        <p>{{ post.content }}</p>
    {% endfor %}
{% endblock %}

{% block stylesheets %}
{% endblock %}
